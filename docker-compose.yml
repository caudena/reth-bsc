services:
  reth-bsc:
    image: "caudenacom/reth_bsc:${BUDDY_EXECUTION_REVISION}"
    container_name: reth-bsc
    restart: unless-stopped
    ports:
      - "${RPC_ADDR}:${METRICS_PORT}:9001"
      - "${RPC_ADDR}:8545:8545"
      - "${RPC_ADDR}:8546:8546"
      - "${NODE_IP}:13454:13454"
      - "${NODE_IP}:13454:13454/udp"
      - "${NODE_IP}:30303:30303"
      - "${NODE_IP}:30303:30303/udp"
    command: node --chain bsc --datadir=/data --http --http.api all --http.addr=0.0.0.0 --http.port=8545 --ws --ws.addr=0.0.0.0 --ws.port=8546 --rpc.max-connections=4096 --rpc.max-response-size=1000 --db.max-size=12TB --db.page-size=8KB --db.read-transaction-timeout=0 --metrics=0.0.0.0:9001 --log.file.directory=/logs --log.file.max-size=200 --log.file.max-files=5 --log.file.filter=info
    volumes:
      - /home/deploy/bsc_data/server/data-seed/data_dir:/data
      - /home/deploy/bsc_node/logs:/logs